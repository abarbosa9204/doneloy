{% set i = 0 %}
{% for item in departamentos %}
    {% set selected = '' %}
    {% if i == 0 %}
        {% set selected = 'selected' %}
    {% endif %}
    <option value="{{ item.id }}" {{selected}}>{{ item.nombre }}</option>
    {% set i = i + 1 %}
{% endfor %}
